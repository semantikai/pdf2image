export const PDF_RESOLUTION = 1.5 * 10 ** 6; // target resolution 1.5 Mpx
export const MAX_PDF_SCALE = 5.5; // max DPI = 500

export const DEFAULT_OPTIONS = {
  allowedMaxPages: Infinity,
  resolution: PDF_RESOLUTION,
};
